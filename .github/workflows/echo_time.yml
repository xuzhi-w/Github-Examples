name: Echo Time

on: 
  schedule:
    - cron: '*/15 * * * *'

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Echo current time
        run: echo "The current server time is $(date)"